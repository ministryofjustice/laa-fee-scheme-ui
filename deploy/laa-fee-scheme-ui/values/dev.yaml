# Default values for laa-fee-scheme-ui.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  name: "laa-fee-scheme-ui-sa"
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  capabilities:
    drop:
      - ALL
  seccompProfile:
    type: RuntimeDefault

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  hosts:
    - host: laa-fee-scheme-ui-dev.cloud-platform.service.justice.gov.uk
      paths:
        - path: /
          pathType: Prefix
  annotations:
    external-dns.alpha.kubernetes.io/set-identifier: "laa-fee-scheme-ui-laa-fee-scheme-ui-dev-green"
    external-dns.alpha.kubernetes.io/aws-weight: "100"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

livenessProbe:
  httpGet:
    path: /status
    port: http
  initialDelaySeconds: 30
  periodSeconds: 60

readinessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 60

env:
  # These will be overridden by deploy.sh with values from GitHub vars/secrets
  SERVICE_NAME: ""
  SERVICE_PHASE: ""
  DEPARTMENT_NAME: ""
  DEPARTMENT_URL: ""
  CONTACT_EMAIL: ""
  CONTACT_PHONE: ""
  SERVICE_URL: ""
  SESSION_SECRET: ""
  SESSION_NAME: ""
  RATELIMIT_HEADERS_ENABLED: ""
  RATELIMIT_STORAGE_URI: ""
  RATE_LIMIT_MAX: ""
  RATE_WINDOW_MS: ""
  NODE_ENV: ""
  API_URL: "http://laa-fee-scheme-api.laa-fee-scheme-api-dev:8080"
  OIDC_SCOPE: "openid profile email"
  CLAIMS_API_SCOPE: 
    name: auth-secret-dev
    key: claims-api-scope
  OIDC_LOGIN_PATH: "/login"
  OIDC_CLIENT_SECRET: 
    name: auth-secret-dev
    key: client-secret
  OIDC_CALLBACK_PATH: "/callback"
  OIDC_LOGOUT_PATH: "/logout"
  AUTH_ENABLED: true
  BASE_URL: "https://laa-fee-scheme-ui-dev.cloud-platform.service.justice.gov.uk"
  CLIENT_ID: 
    name: auth-secret-dev
    key: client-id
  USE_SSL: true
  ISSUER_BASE_URL:
    name: auth-secret-dev
    key: issuer-url
  REDIS_HOST:
    name: claimandassess-assess-redis
    key: primary_endpoint_address
  REDIS_AUTH_TOKEN:
    name: claimandassess-assess-redis
    key: auth_token  

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}
