apiVersion: v1
kind: Service
metadata:
  name: {{ include "laa-fee-scheme-ui.fullname" . }}
  labels:
    {{- include "laa-fee-scheme-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laa-fee-scheme-ui.selectorLabels" . | nindent 4 }}
