# API configuration
# Override these in .env.local for local development (git-ignored)
# For deployed environments, pass as Docker build args: --build-arg VITE_API_URL=... --build-arg VITE_API_TOKEN=...
VITE_API_URL=http://localhost:8285/api/v1/advocacy-fee-request
VITE_API_TOKEN=some-test-token
